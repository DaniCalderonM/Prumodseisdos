<div class="bg-light p-2 text-dark bg-opacity-75 col-5 text-center">
<h2><u>Iniciar Sesión</u></h2>
<strong> Si no tienes una cuenta creada, recuerda hablar con Esteban para que te cree una </strong>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field mt-3">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= f.label :password %><br />
    <%= f.password_field :password, autocomplete: "Contraseña Actual" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end %>

  <div class="actions mt-2 mb-2">
    <%= f.submit "Iniciar Sesión" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>


</div>